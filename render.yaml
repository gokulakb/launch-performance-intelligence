services:
  - type: web
    name: launch-performance-intelligence
    runtime: image
    plan: free
    region: ohi
    dockerfilePath: ./Dockerfile
    envVars:
      - key: RENDER
        value: true
      - key: PORT
        value: 10000
    healthCheckPath: /_stcore/health
    autoDeploy: true
